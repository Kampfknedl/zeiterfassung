# Force single Kotlin version to resolve duplicate class conflicts
kotlin.version=1.8.22

# Gradle optimizations for GitHub Actions
org.gradle.jvmargs=-Xmx2048m
org.gradle.parallel=true
org.gradle.workers.max=2
